*** Settings ***
Library             Selenium2Library
Test Setup          Open Sendmail Browser
Test Teardown       Close Browser

*** Variables ***
${SENDMAIL_URL}     http://earth.local:8080/pennymail/sendmail

*** Test Cases ***

Valid Email Should Be Send
    [Tags]                             not_ready
    Send Email About New Gadget To     bomb412355451244@gmail.com,max412355451244@gmail.com
    Everything Is OK
    
Invalid Email Should Be Inform To Penny
    [Tags]                             not_ready
    Send Email About New Gadget To     bomb412355451244@gmail.com,Invalid.com,Invalid@com
    Penny Should See Message           แสรดด e Penny โง่ Invalid.com ใช้ไม่ได้
    Penny Should See Message           แสรดด e Penny โง่ Invalid@com ใช้ไม่ได้

*** Keywords ***
Send Email About New Gadget To
	[Arguments]                 ${recipients}
	Fill in Recipients          ${recipients}
    Fill in Topic               New Gadget Coming     
    Fill in Message             New Gadget arrive, Order Now !
    Click Button                send
      
Open Sendmail Browser
	Open Browser                  ${SENDMAIL_URL} 
    Wait Until Page Contains      RECIPIENTS

Fill in Recipients
	[Arguments]     ${recipients}
    Input Text      recipients      ${recipients}

Fill in Topic
	[Arguments]     ${topic}
    Input Text      topic           ${topic}

Fill in Message
	[Arguments]     ${message}
    Input Text      message         ${message}

Penny Should See Message
    [Arguments]     ${message}
    Page Should Contain             ${message}
    
Everything Is OK
    Page Should Not Contain         แสรดด e Penny โง่